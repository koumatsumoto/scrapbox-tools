import { highlightTags } from './highlight-tags';

export const enableCustomListItem = () => {
  /**
   * Remove hash and add custom css class to link-text in list-item
   * Add custom css class to time text in list-item
   */
  highlightTags();
};
